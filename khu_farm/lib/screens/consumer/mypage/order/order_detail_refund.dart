import 'package:flutter/material.dart';

class ConsumerRefundScreen extends StatelessWidget {
  const ConsumerRefundScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('환불'),
        centerTitle: true,
      ),
    );
  }
}
